[package]
name = "themoretheless-tokenizer-logfmt"
version = "0.4.0"
edition = "2024"
authors = ["Denis Mezhov"]
description = "Full logfmt engine (lex/parse/validate) for themoretheless-tokenizer"
license = "MIT OR Apache-2.0"
repository = "https://github.com/themoretheless/tokenizer"
readme = "README.md"
publish = ["crates-io"]
keywords = ["tokenizer", "lexer", "logfmt", "structured-logging", "parser"]
categories = ["parsing", "text-processing"]

[lib]
name = "themoretheless_tokenizer_logfmt"
path = "src/lib.rs"

[dependencies]
themoretheless-tokenizer-core = { path = "../tokenizer-core", version = "0.4.0" }

[package.metadata.docs.rs]
all-features = true
