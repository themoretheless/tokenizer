# themoretheless-tokenizer-go

**Full** Go plugin: lossless lex, recovering parse, shared AST (`Module`/`Item`/`Stmt`/`Expr`),
semantic tokens, diagnostics.

Uses `themoretheless-tokenizer-core::fullkit` with a language profile.
