# themoretheless-tokenizer-url

Lossless URL / path highlighting tokens and lightweight validation for editors.

Depends only on `themoretheless-tokenizer-core`. Re-exported from the facade as
`themoretheless_tokenizer::url` and crate-root helpers (`tokenize_url`, …).
